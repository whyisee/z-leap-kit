# 搜索组件 TODO

> 目标：把搜索组件从“能搜到”升级成“比编辑器默认搜索更懂个人知识库”。

## 已完成

- [x] 支持 Markdown、代码、配置、文本等多类型文件索引。
- [x] 支持 `.leap` 下四象限任务、日历事项、收件箱等插件数据搜索。
- [x] 支持 `@docs`、`@code`、`@prompt`、`@task`、`@calendar`、`@inbox`、`@project`、`path:`、`recent:`、`#tag` 查询语法。
- [x] 扩展搜索命令：`@latest`、`@favorite`、`@recent`、`@todo`、`@done`、`@today`、`@week`、`@month`、`limit:`、`ext:`、`title:`、`source:`、`after:`、`before:`。
- [x] 支持搜索结果分组、排序、打开文件定位行号、复制 Prompt、收藏、转任务、完成任务等动作。
- [x] 支持 `@` 命令快捷提示，减少手动记语法。
- [x] 支持搜索历史，持久化到 `.leap/components/search-history.json`。

## 本轮补齐

- [x] 搜索结果关键词高亮。
- [x] 搜索结果预览展开。
- [x] 索引范围配置：扩展名、排除规则、文件大小上限。
- [x] 工作区文件变化后自动刷新索引。
- [x] 可选 AI 查询理解：把自然语言查询改写成 Leap Search 语法。

## 后续增强

- [ ] 语义向量检索：解决“关键词不一致但语义相同”的召回问题。
- [ ] 搜索调试面板：展示命中字段、得分、过滤器、索引耗时。
- [ ] 搜索质量评测集：沉淀典型查询和期望结果，用于回归。
- [ ] 大仓库增量索引：避免每次刷新都全量扫描。


## 卡住问题

- 2026-06-01: 当前开发卡住的第一个具体问题是什么？待通过番茄钟写下。
